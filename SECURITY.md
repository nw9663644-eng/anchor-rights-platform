# 平台安全与人工复核说明

本平台采用分层防护，而不是把“防火墙”当作单一页面功能。

## 已实现

- 连续登录失败持久化计数，默认 5 次失败后锁定 15 分钟。
- 会话使用 HttpOnly、SameSite Cookie，前端脚本无法读取会话令牌。
- 新上传证据使用 Fernet 对称加密保存，并记录 SHA-256 完整性指纹。
- AI 提问发送前自动脱敏身份证号、手机号、银行卡号和邮箱；本站不保存 AI 对话正文。
- 登录、密码修改、评估、证据、AI 使用、内容维护和人工复核均记录审计事件。
- 每份评估报告可提交人工复核工单，保存状态、复核人、意见和时间。
- API 默认关闭调试文档，并发送 CSP、禁止嵌入、MIME 防嗅探等安全响应头。

## 服务器上线要求

1. 将 `deploy/nginx-security.conf` 按实际部署路径安装为 Nginx 站点配置。
2. 执行 `deploy/harden-ubuntu.sh` 前确认 SSH 端口仍为 22；若使用自定义端口，应先单独放行。
3. 正式对外服务必须配置域名与 HTTPS，并在后端环境变量中设置 `COOKIE_SECURE=true`。
4. 将 `backend/.data_key` 与数据库分开加密备份。密钥丢失后，加密证据无法恢复。
5. 定期更新系统依赖、备份数据库，并复核管理员和人工复核人员权限。

## 能力边界

应用层加密、防火墙和审计可以降低数据泄露与滥用风险，但不能代替持续漏洞修复、权限管理、备份恢复和安全事件响应。人工复核用于核对事实、计分与依据引用，不自动等同于执业律师法律意见。
